# Improvements


## Manager
- About all tables, must highlight the Attributes rows, that is blended with values rows. Also apply pagination, do not just a full of items

### Shop manager: 
- Create/Update product/sku does not have image upload yet
- Shop order must have details action button, show how order process from start to now, list all items in order,...
- Shop inventory API already update to return SKU and product, must optimize the table


### Delivery Agent:
- When login as Delivery Agent, redirect them into manage delivery. Does not need to how home page, they roles used for handle orders' status only
- Also list down orders as table, filter by order status